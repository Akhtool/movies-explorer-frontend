import './App.css';
// App — корневой компонент приложения, его создаёт CRA.
const App = (props) => {
    return (
        <div>
            <h1>Hello World!</h1>
        </div>
    );
};

export default App;